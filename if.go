package main

import (
	// "./greeting"
	"fmt"
)

func main() {

	// func包成import package以後執行要加namespace
	// var greeter = greeting.Greeter{"John"}
	// greeting.Greet(greeter.Name, true)
	// greeting.Pain()
	a := 1
	switch a {
	case 1:
		fmt.Println(nil)
		break
		fmt.Println(true)
	}
}
